module feed (
    input wire next_color, clk, rst_b,
    output wire s_j, s_k, term_valid, fetch_done,
    output wire wire [1:0] term_type,
    output wire signed [7:0] weight
);
    reg index;
    
    

endmodule